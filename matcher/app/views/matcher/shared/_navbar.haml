%nav.navbar.navbar-inverse.navbar-fixed-top(role="navigation")
  .container
    .navbar-header
      %button.navbar-toggle(type="button" data-toggle="collapse" data-target="#navbar-collapse")
        %span.sr-only Toggle
        %span.icon-bar/
        %span.icon-bar/
        %span.icon-bar/
      - if user_signed_in?
        = link_to "Branch", main_app.feed_items_path, :class => 'navbar-brand'
      - else
        = link_to "Branch", main_app.root_url, :class => 'navbar-brand'

    #navbar-collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav
        - if user_signed_in?
          %li= link_to "Feed", main_app.feed_items_path
          %li.dropdown
            = link_to raw('Compose <b class="caret"></b>'), "#", 
              :class => 'dropdown-toggle',
              'data-toggle' => 'dropdown'

            %ul.dropdown-menu
              %li= link_to 'New Proposal', main_app.new_proposal_path
              %li= link_to 'New Discussion', main_app.new_discussion_topic_path
        -else
          %li= link_to "About", main_app.pages_path('about')

      = render partial: 'devise/sessions/navbar_form'
